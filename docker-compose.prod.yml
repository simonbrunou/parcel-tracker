services:
  parcel-tracker:
    image: ghcr.io/simonbrunou/parcel-tracker:main
    ports:
      - "8080:8080"
    volumes:
      - parcel-data:/data
    environment:
      - PARCEL_TRACKER_PASSWORD=changeme
    restart: unless-stopped

volumes:
  parcel-data:
